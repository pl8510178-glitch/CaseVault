NODE_ENV=development
PORT=8787
# Generate 32 random bytes (64 hex characters) for each value. Do not commit .env.
DATA_ENCRYPTION_KEY=replace_with_64_hex_characters
AUDIT_HMAC_KEY=replace_with_different_64_hex_characters
# Development only. In production, use a real identity approval process instead.
DEV_SEED_ADMIN_PASSWORD=replace_with_a_long_unique_password